# PersonaX Docker runtime direct dependencies.
# Pinning the direct runtime surface keeps different machines on the validated
# framework versions while the Docker image fixes the OS and Python runtime.
openai==3.5.0
pydantic==2.13.4
PyYAML==6.0.3
langgraph==1.2.11
langgraph-checkpoint-sqlite==3.1.1
langchain-core==1.6.0
streamlit==1.62.0
fastapi==0.141.1
uvicorn==0.52.4
pillow==12.0.0
